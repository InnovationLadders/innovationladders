import React from 'react';

const SiteSettingsManager: React.FC = () => {
  return (
    <div>
      <p>SiteSettingsManager component - Not in use (settings are now static)</p>
    </div>
  );
};

export default SiteSettingsManager;
