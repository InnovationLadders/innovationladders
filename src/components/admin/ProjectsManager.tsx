import React from 'react';

const ProjectsManager: React.FC = () => {
  return (
    <div>
      <p>ProjectsManager component - Not in use (data is now static)</p>
    </div>
  );
};

export default ProjectsManager;
