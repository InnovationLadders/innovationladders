import React from 'react';

const ServicesManager: React.FC = () => {
  return (
    <div>
      <p>ServicesManager component - Not in use (data is now static)</p>
    </div>
  );
};

export default ServicesManager;
